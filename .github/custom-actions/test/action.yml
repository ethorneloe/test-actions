name: "test"
description: "Test"

runs:
  using: "composite"
  steps:
    - name: "Deploy changed PowerShell modules"
      shell: pwsh
      run: |
        $en = '${{ github.job.environment }}'
        write-output $en
