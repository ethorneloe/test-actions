name: "test"
description: "Test"

runs:
  using: "composite"
  steps:
    - name: "Deploy changed PowerShell modules"
      shell: pwsh
      run: |
        write-output '${{ github.job.environment }}'
